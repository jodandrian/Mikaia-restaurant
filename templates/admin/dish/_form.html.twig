{# Start building form HTML element #}
{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}

    <div>
        {{ form_label(form.name, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
        {{ form_widget(form.name) }}
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
            {{ form_label(form.category, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
            {{ form_widget(form.category) }}
        </div>
        <div>
            {{ form_label(form.price, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
            {{ form_widget(form.price) }}
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
            {{ form_label(form.badge, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
            {{ form_widget(form.badge) }}
        </div>
        <div>
            {{ form_label(form.imageUrl, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
            {{ form_widget(form.imageUrl) }}
        </div>
    </div>

    <div>
        {{ form_label(form.description, null, {'label_attr': {'class': 'block text-xs uppercase tracking-wider font-semibold mb-2 text-stone-300'}}) }}
        {{ form_widget(form.description) }}
    </div>

    <button class="bg-[#8CD62B] text-black font-bold px-6 py-3 rounded-xl hover:bg-opacity-90 transition mt-4 w-full md:w-auto">
        {{ button_label|default('Save Dish') }}
    </button>

{# End form rendering and inject CSRF hidden field automatically #}
{{ form_end(form) }}